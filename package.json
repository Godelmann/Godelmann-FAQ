{
  "name": "godelmann-faq",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "release:patch": "npm version patch --no-git-tag-version && npm run release:commit",
    "release:minor": "npm version minor --no-git-tag-version && npm run release:commit",
    "release:commit": "git add package.json package-lock.json && git commit -m \"chore(release): $npm_package_name v$npm_package_version\""
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.12.0",
    "eslint": "^9.39.4",
    "globals": "^17.4.0",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.57.0",
    "vite": "^8.0.1"
  }
}
